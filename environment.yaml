name: epi2melabs-wf-cas9
channels:
    - epi2melabs
    - bioconda
    - conda-forge
    - defaults
dependencies:
    - python==3.8.*
    - aplanat >=0.5.0
    - pysam
    - fastcat
    - epi2melabs
    - bedtools
    - pybedtools
    - samtools
    - natsort
